@using Carmasters.Core.Application.Configuration
@using Carmasters.Core.Application.Model
@using Carmasters.Core.Domain
@using Carmasters.Http.Api
@model PricingOptions
@* The garage's own tax wording lives in its settings: nothing about VAT is asserted here. *@
@if (!string.IsNullOrWhiteSpace(Model.Invoice?.Disclaimer))
{
    <p class="doc-line" style="white-space: pre-line;">@Model.Invoice.Disclaimer</p>
}
